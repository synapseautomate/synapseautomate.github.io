# Workflow Output Schema v2

Public, synthetic schema example for source-aware workflow outputs. It is not customer data and does not claim production accuracy. Critical fields must carry evidence or remain explicit unknowns. Amount, currency, date, unit and company identifiers use normalized fields.

The included 10 cases are synthetic validator fixtures only.
